%h2 All Movies

%table#movies
  %thead
    %tr
      %th= link_to( "Movie Title", "/movies?sort=title" , {:id=>"Title_Header" , :class=> (@hint == 'title' ?"hilite" : "")})
      %th Rating 
      %th= link_to "Release Date", "/movies?sort=date", {:id=>"release_date_header", :class=> (@hint == 'date' ?"hilite" : "")}
      %th More Info
  %tbody
    - @movies.each_with_index do |movie,count|
      %tr{:class => oddness(count)}
        %td= movie.title 
        %td= movie.rating
        %td= movie.release_date
        %td= link_to "More about #{movie.title}", movie

%ul#page_nav
  %li= link_to 'Add New Movie Manually', new_movie_path

